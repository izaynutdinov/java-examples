1. Create database running database.sql on MySQL being logged in as root
2. Build project running: mvn install
3. Copy target/binadox-chat-1.0.war to $CATALINA_HOME/webapps/binadox-chat.war

Program was tested under Tomcat 8.0.28

1. Создайте базу выполнив database.sql на MySQL под root'ом
2. Скомпилируйте проект выполнив: mvn install
3. Скопируйте target/binadox-chat-1.0.war в $CATALINA_HOME/webapps/binadox-chat.war

Программа тестировалась только под Tomcat 8.0.28
